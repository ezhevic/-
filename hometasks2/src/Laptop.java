public class Laptop extends PortableComputer {
    public Laptop (String maker, String model, String cpu, String ram, String disk, String monitor, String keyboard) {
        super(maker, model, cpu, ram, disk, monitor, keyboard);
    }
}
