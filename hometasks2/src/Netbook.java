public class Netbook extends PortableComputer {
    public Netbook (String maker, String model, String cpu, String ram, String disk, String monitor, String keyboard) {
        super(maker, model, cpu, ram, disk, monitor, keyboard);
    }
}
